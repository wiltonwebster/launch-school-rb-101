numbers = [1, 2, 3, 4, 5]

# Explain the difference between:

numbers.delete_at(1)
numbers.delete(1)

# The first method will destructively remove the element at index 1.
# The second method will destructively remove every element that equals 1.